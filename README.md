This activity focuses on:
Regex-based Commit Validation: Define commit message rules and validate PR commits.
Outputs and Logging: Clearly display invalid commits in PR checks.
Failing PR Checks: Prevent merge if commits do not meet standards.
Multi-Environment Reuse: Workflow can be reused across multiple repositories.
